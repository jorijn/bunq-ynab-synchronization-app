imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: en

framework:
    #esi: ~
    #translator: { fallbacks: ['%locale%'] }
    secret: '%secret%'
    router:
        resource: '%kernel.project_dir%/app/config/routing.yml'
        strict_requirements: ~
    form: ~
    csrf_protection: ~
    validation: { enable_annotations: true }
    #serializer: { enable_annotations: true }
    default_locale: '%locale%'
    trusted_hosts: ~
    session:
        # https://symfony.com/doc/current/reference/configuration/framework.html#handler-id
        handler_id: session.handler.native_file
        save_path: '%kernel.project_dir%/var/sessions/%kernel.environment%'
    fragments: ~
    http_method_override: true
    assets: ~
    php_errors:
        log: true

jorijn_symfony_bunq:
    production_config_location: '%jorijn_symfony_bunq.production_config_location%'
    sandbox_config_location: '%jorijn_symfony_bunq.sandbox_config_location%'
    environment: '%jorijn_symfony_bunq.environment%'
    application_description: '%jorijn_symfony_bunq.application_description%'
    allowed_ips: '%jorijn_symfony_bunq.allowed_ips%'

jorijn_ynab_bunq_connector:
  api_key: '%jorijn_ynab_bunq_connector.api_key%'
  connections: '%jorijn_ynab_bunq_connector.connections%'